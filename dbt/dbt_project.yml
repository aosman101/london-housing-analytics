name: "london_housing_analytics"
version: "1.0.0"
config-version: 2

profile: "housing_warehouse"

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  london_housing_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table
